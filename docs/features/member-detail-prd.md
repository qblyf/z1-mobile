# 会员中心模块 · 详细 PRD

> **模块**：会员中心
> **版本**：v1.0
> **日期**：2026-05-16
> **状态**：初稿
> **依据**：HTML 原型 + feature-list.md + api-spec.md

> **⚠️ 类型唯一真实源**：API 字段定义以 `lib/types/api/` 为准（相关：member-types.dart）。本 PRD 不复制具体字段名/类型。

---

## 一、页面路径总览

```
/member/home              → 会员首页（搜索、列表、最近访问）
        ↓
/member/:memberId        → 会员详情（基本信息、消费记录、积分）
        ↓
/member/add              → 新增会员
        ↓
/member/creditscore      → 积分查询
        ↓
/member/creditscore/edit → 积分调整
```

---

## 二、页面 1：会员首页

### 2.1 路由

```
路径：/member/home
名称：会员中心
Tab：会员 Tab
```

### 2.2 基本布局

```
┌──────────────────────────────────┐
│ ← 会员中心                       │
├──────────────────────────────────┤
│ [🔍 搜索手机号/姓名...]    [新增] │  ← 搜索栏 + 新增入口
├──────────────────────────────────┤
│                                  │
│  最近服务                        │  ← 最近访问的会员
│  ┌────┐ ┌────┐ ┌────┐           │
│  │张3 │ │李4 │ │王5 │           │
│  └────┘ └────┘ └────┘           │
│                                  │
│  全部会员                        │  ← 会员列表
│  ┌──────────────────────────┐   │
│  │ 👤 张三  金卡  积分 28888  │   │
│  ├──────────────────────────┤   │
│  │ 👤 李四  银卡  积分 12000  │   │
│  ├──────────────────────────┤   │
│  │ 👤 王五  普通  积分 3500   │   │
│  └──────────────────────────┘   │
│                                  │
│  [加载更多...]                   │
│                                  │
└──────────────────────────────────┘
```

### 2.3 核心交互逻辑

#### 搜索

- 输入手机号或姓名
- 调用 `/members/list` 接口，keyword 参数
- 实时搜索（300ms 防抖）或点击搜索按钮
- 有结果 → 高亮匹配项
- 无结果 → 显示"未找到该会员"

#### 新增会员

- 点击右上角"新增"按钮
- 跳转 `/member/add`

#### 会员列表

- 默认显示最近访问的会员（最近 5 条）
- 点击会员卡片 → 跳转 `/member/:memberId`
- 下拉刷新、上拉加载更多

#### 快捷操作（入口）

- 从首页 Tab 进来时显示快捷操作
- 扫码查会员、快捷开单等

### 2.5 异常/边界情况

| 场景 | 处理 |
|------|------|
| 搜索为空 | 显示全部会员（默认列表） |
| 会员不存在 | 显示空状态"未找到该会员" |
| 网络错误 | 显示错误页，点击重试 |
| 无会员数据 | 显示空状态 + "新增会员"引导 |

### 2.6 跳转关系

| 来源 | 触发 | 目标 |
|------|------|------|
| TabBar | 点击会员 Tab | /member/home |
| /member/home | 点击"新增" | /member/add |
| /member/home | 点击会员卡片 | /member/:memberId |
| /member/home | 输入搜索 | 调用 API，列表更新 |

---

## 三、页面 2：会员详情

### 3.1 路由

```
路径：/member/:memberId
名称：会员信息
参数：memberId
```

### 3.2 基本布局

```
┌──────────────────────────────────┐
│ ← 会员信息              [编辑]   │
├──────────────────────────────────┤
│                                  │
│  ┌────────────────────────────┐  │
│  │ 头像  姓名  金卡会员        │  │  ← 会员卡片
│  │ 138****1234               │  │
│  │ 注册于 2023-01-15           │  │
│  │ 标签：老客户 | 高价值 | 活跃 │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 可用积分：28,888          │  │  ← 积分卡片
│  │ 可抵 ¥288.88              │  │
│  │ [积分查询]    [积分调整]   │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 快捷操作                   │  │
│  │ 📋消费记录 📃优惠券 💰储值 │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 今日消费                   │  │
│  │ ¥3,056 今天  ¥8,200 本周   │  │
│  │ [查看全部 >]               │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 消费记录列表               │  │
│  │ ├─ 订单号  商品  ¥2,800    │  │
│  │ └─ 2026-05-15             │  │
│  └────────────────────────────┘  │
│                                  │
└──────────────────────────────────┘
```

### 3.3 核心交互逻辑

#### 会员卡片

- 显示：头像/姓名首字母、姓名、等级标签、手机号、注册时间
- 标签：老客户/高价值/活跃 等
- 点击"编辑" → 编辑会员信息

#### 积分操作

- 点击"积分查询" → 跳转 `/member/creditscore?memberId=xxx`
- 点击"积分调整" → 跳转 `/member/creditscore/edit?memberId=xxx`

#### 快捷操作

- 消费记录：跳转订单列表（按会员筛选）
- 优惠券：查看该会员可用优惠券
- 储值：储值记录（预留）
- 备注：添加会员备注

#### 消费记录

- 显示最近消费（今日/本周/本月汇总）
- 点击"查看全部" → 跳转订单列表（该会员）

#### 编辑会员

- 跳转编辑页（`/member/edit?memberId=xxx`）

### 3.4 字段说明

### 3.5 异常/边界情况

| 场景 | 处理 |
|------|------|
| 会员不存在/已注销 | 跳转回列表，显示 Toast "该会员不存在" |
| 积分加载中 | 显示骨架屏 |
| 无消费记录 | 显示空状态"暂无消费记录" |
| 网络错误 | 显示错误页，点击重试 |

### 3.6 跳转关系

| 来源 | 触发 | 目标 |
|------|------|------|
| /member/home | 点击会员卡片 | /member/:memberId |
| /member/:memberId | 点击"积分查询" | /member/creditscore |
| /member/:memberId | 点击"积分调整" | /member/creditscore/edit |
| /member/:memberId | 点击"编辑" | /member/edit |
| /member/:memberId | 点击消费记录项 | /order/:orderNumber |

---

## 四、页面 3：新增会员

### 4.1 路由

```
路径：/member/add
名称：新增会员
```

### 4.2 基本布局

```
┌──────────────────────────────────┐
│ ← 新增会员                       │
├──────────────────────────────────┤
│                                  │
│  ┌────────────────────────────┐  │
│  │ 手机号 *  [必填]            │  │  ← 必填项
│  └────────────────────────────┘  │
│  ┌────────────────────────────┐  │
│  │ 姓名                       │  │
│  └────────────────────────────┘  │
│  ┌────────────────────────────┐  │
│  │ 性别    ○男  ○女  ○未知    │  │
│  └────────────────────────────┘  │
│  ┌────────────────────────────┐  │
│  │ 生日    [选择日期]         │  │
│  └────────────────────────────┘  │
│  ┌────────────────────────────┐  │
│  │ 会员等级  [下拉选择]        │  │
│  └────────────────────────────┘  │
│  ┌────────────────────────────┐  │
│  │ 初始积分                   │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │         [保存]             │  │
│  └────────────────────────────┘  │
└──────────────────────────────────┘
```

### 4.3 核心交互逻辑

#### 表单验证

- 手机号：必填，11 位数字，唯一性校验（调用 API）
- 姓名：选填
- 性别：默认"未知"
- 生日：选填，日期选择器
- 会员等级：选填，默认普通会员
- 初始积分：选填，默认 0

#### 手机号唯一性

- 输入手机号后失焦/提交时调用 `/members/check-phone`
- 已存在 → 提示"该手机号已注册"，阻止提交
- 不存在 → 正常提交

#### 保存

- 点击"保存"按钮
- 调用 `POST /members/add`
- 成功 → 跳转会员详情页 `/member/:memberId`
- 失败 → 显示错误提示

### 4.5 异常/边界情况

| 场景 | 处理 |
|------|------|
| 手机号格式错误 | 输入框红色边框 + 提示"请输入正确的11位手机号" |
| 手机号已注册 | 提示"该手机号已注册"，不可重复提交 |
| 必填项未填 | 保存按钮禁用 |
| 网络错误 | 显示错误提示，可重试 |
| 保存失败 | 停留在当前页，显示具体错误原因 |

### 4.6 跳转关系

| 来源 | 触发 | 目标 |
|------|------|------|
| /member/home | 点击"新增" | /member/add |
| /member/add | 保存成功 | /member/:memberId（自动）|
| /member/add | 点击顶部返回 | /member/home |

---

## 五、页面 4：积分查询

### 5.1 路由

```
路径：/member/creditscore
名称：积分查询
参数：memberId
```

### 5.2 基本布局

```
┌──────────────────────────────────┐
│ ← 积分查询                       │
├──────────────────────────────────┤
│                                  │
│  ┌────────────────────────────┐  │
│  │ 会员：张三  金卡会员        │  │
│  │ 可用积分：28,888 分        │  │
│  │ 可抵 ¥288.88               │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 总积分  28,888             │  │
│  │ 可用  25,000               │  │
│  │ 已用  3,888                │  │
│  │ 冻结  0                   │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 积分明细                   │  │
│  │                            │  │
│  │ +2000  消费获积分  05-15   │  │
│  │ -200   积分抵扣    05-14   │  │
│  │ +5000  调整增加    05-10   │  │
│  │ +23888 注册初始    01-15   │  │
│  │                            │  │
│  └────────────────────────────┘  │
│                                  │
└──────────────────────────────────┘
```

### 5.3 核心交互逻辑

#### 积分概览

- 显示：总积分、可用积分、已用积分、冻结积分
- 可抵金额 = 可用积分 / 100（积分规则：100分=¥1）

#### 积分明细列表

- 按时间倒序排列
- 每条显示：变动积分（+/−）、变动原因、日期
- 类型标识：consume（消费获积分）/ refund（退款返还）/ adjust（调整）/ expire（过期）

#### 下拉刷新

- 下拉刷新加载最新积分记录

### 5.4 字段说明

### 5.5 异常/边界情况

| 场景 | 处理 |
|------|------|
| 无积分记录 | 显示空状态"暂无积分变动记录" |
| 会员不存在 | 返回列表，显示提示 |
| 网络错误 | 显示错误页，点击重试 |

### 5.6 跳转关系

| 来源 | 触发 | 目标 |
|------|------|------|
| /member/:memberId | 点击"积分查询" | /member/creditscore |
| /member/creditscore | 点击"积分调整" | /member/creditscore/edit |
| /member/creditscore | 点击顶部返回 | /member/:memberId |

---

## 六、页面 5：积分调整

### 6.1 路由

```
路径：/member/creditscore/edit
名称：积分调整
参数：memberId
```

### 6.2 基本布局

```
┌──────────────────────────────────┐
│ ← 积分调整                       │
├──────────────────────────────────┤
│                                  │
│  ┌────────────────────────────┐  │
│  │ 当前积分：25,000           │  │
│  │ 可用余额：¥250.00          │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 调整方向                   │  │
│  │ ○ 增加  ○ 减少            │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 调整积分   [输入框]        │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 原因       [下拉选择]      │  │
│  │ → 手工调账 / 赔偿 / 活动   │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 备注                       │  │
│  │ [选填]                    │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │      [确认调整]            │  │
│  └────────────────────────────┘  │
│                                  │
└──────────────────────────────────┘
```

### 6.3 核心交互逻辑

#### 显示当前积分

- 只读显示当前可用积分和可抵金额

#### 选择调整方向

- 增加 / 减少（二选一）
- 默认选中"增加"

#### 输入调整积分

- 数字输入，最小 1
- 输入后显示调整后余额预览

#### 选择原因

- 下拉选择：手工调账 / 赔偿 / 活动奖励 / 其他
- 必选

#### 备注（选填）

- 最多 200 字符

#### 确认调整

- 点击"确认调整"
- 调用 `POST /members/experience/edit`
- 成功 → 返回会员详情页，Toast "积分调整成功"
- 失败 → 显示错误提示

### 6.4 字段说明

### 6.5 异常/边界情况

| 场景 | 处理 |
|------|------|
| 减少积分 > 可用积分 | 提示"积分不足，无法减少"，按钮禁用 |
| 调整积分为 0 | 按钮禁用 |
| 原因未选 | 按钮禁用 |
| 网络错误 | 显示错误提示，可重试 |
| 并发冲突 | 提示"积分已被其他操作修改，请重试" |

### 6.6 跳转关系

| 来源 | 触发 | 目标 |
|------|------|------|
| /member/creditscore | 点击"积分调整" | /member/creditscore/edit |
| /member/:memberId | 点击"积分调整" | /member/creditscore/edit |
| /member/creditscore/edit | 调整成功（自动）| /member/creditscore |
| /member/creditscore/edit | 点击顶部返回 | /member/:memberId |

---

## 七、模块数据流

```
会员首页 → 搜索/选择会员 → 会员详情 → 积分查询/积分调整/新增会员

API 调用序列：
1. GET  /members/list              → 获取会员列表
2. GET  /member/specified         → 获取会员详情（含积分）
3. POST /members/add               → 新增会员
4. GET  /members/self              → 获取当前会员积分（experience 字段）
5. POST /members/experience        → 调整积分
```

---

## 八、接口清单

> **注意**：金额字段单位为分（cent），非元。

| 页面 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 会员首页 | `/members/list` | GET | 会员列表（keyword 搜索）✅ |
| 会员首页 | `/members/recent` | GET | 最近访问会员 ❌ **接口缺失** |
| 会员详情 | `/member/specified` | GET | ⚠️ **注意路径是 member（单数），参数 userIdents** |
| 会员详情 | `/order/shop-sale-list` | GET | 会员消费记录（按 customerIdent 筛选）✅ |
| 新增会员 | `/members/add` | POST | 新增会员 ✅ |
| 新增会员 | `/members/check-phone` | POST | 手机号唯一性校验 ❌ **接口缺失**（后端未实现）|
| 积分查询 | `/members/self` | GET | 获取当前会员积分（experience 字段）✅ |
| 积分调整 | `/members/experience` | POST | 调整会员积分 ✅ |
| 积分明细 | `/members/experience-log` | — | ❌ **接口缺失**（后端未实现）|

---

## 九、状态流转

会员模块**无业务状态机**（不像订单/审批）。涉及的会员状态字段：

| 字段 | 含义 | 取值 |
|------|------|------|
| `state` | 会员启用/停用 | `1=启用 / 0=停用`（具体值见 `member-types.dart`） |
| `levelID` | 会员等级 | 关联 `/member-level/detail-or-all` |
| `experience` | 积分余额 | int，可通过 `/members/experience` 调整 |

> 详细字段定义见 `member-types.dart`。

---

## 十、模块关联

```
┌──────────────────────────────────────────────────────┐
│                   会员模块关联                        │
├──────────────────────────────────────────────────────┤
│                                                      │
│        ┌──────────────┐                             │
│        │  会员首页 /member │                          │
│        └──────┬───────┘                             │
│               │ 点击列表项                            │
│               ↓                                      │
│        ┌──────────────┐    ┌────────────┐          │
│        │  会员详情     │───→│ 信用评分   │          │
│        │  /member/:id  │    │ 信用评分编辑│         │
│        └──────┬───────┘    └────────────┘          │
│               │                                      │
│               │ 点击订单                              │
│               ↓                                      │
│        订单详情 /order/:orderNumber                  │
│                                                      │
│        新增会员 ───创建成功───→ 会员详情              │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 10.1 模块跳转

| 来源 | 触发 | 目标 | 来源代码 |
|------|------|------|---------|
| `/member` | 点击会员列表项 | `/member/:id` | `member_home_page.dart:178` |
| `/member/add` | 新增会员成功后 | `/member/:id` | `member_add_page.dart:370` |
| `/member/:id` | 点击"信用评分"按钮 | `/member/:id/creditscore` | `member_detail_page.dart:281` |
| `/member/:id` | 点击"编辑信用评分" | `/member/:id/creditscore/edit` | `member_detail_page.dart:288` |
| `/member/:id` | 点击订单列表项 | `/order/:orderNumber` | `member_detail_page.dart:489` |

### 10.2 数据共享

| 数据 | 来源 | 消费者 |
|------|------|--------|
| `userIdent`（会员 ID） | 会员首页搜索/列表 | 会员详情查询 |
| `experience` | 积分查询 | 积分调整页 / 会员卡显示 |
| `levelID` | 会员详情 | 会员等级展示 / 权益判断 |
| `mobilePhone` | 会员详情 | 零售开单会员搜索（最近会员） |

---

## 十一、待确认事项

1. 会员等级列表接口（新建会员时需获取等级下拉选项）
2. 积分抵扣规则（100分=¥1，最高抵扣比例）
3. 会员标签体系（老客户/高价值/活跃 如何生成）