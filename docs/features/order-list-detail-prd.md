# 订单列表模块 · 详细 PRD

> **模块**：订单管理（销售订单）
> **版本**：v1.0
> **日期**：2026-05-17
> **状态**：初稿
> **依据**：feature-list.md + api-endpoints.dart

---

## 一、页面路径总览

```
/order/list               → 订单列表
        ↓
/order/:orderNumber      → 订单详情
```

---

## 二、页面 1：订单列表

### 2.1 路由

```
路径：/order/list
名称：订单列表
父级：首页（/home）
```

### 2.2 基本布局

```
┌──────────────────────────────────┐
│ ← 销售订单                       │
├──────────────────────────────────┤
│                                  │
│  时间筛选：                      │
│  [今日] [本周] [本月] [全部]     │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 单号：XS202605170001       │  │
│  │ 会员：李四  手机：138****88 │  │
│  │ 金额：¥12,800.00          │  │
│  │ 状态：[已完成] ✓          │  │
│  │ 时间：2026-05-17 10:30    │  │
│  │ 品项：3                    │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 单号：XS202605160002       │  │
│  │ 会员：王五  手机：139****66 │  │
│  │ 金额：¥8,600.00            │  │
│  │ 状态：[进行中]             │  │
│  │ 时间：2026-05-16 14:20    │  │
│  │ 品项：2                    │  │
│  └────────────────────────────┘  │
│                                  │
└──────────────────────────────────┘
```

### 2.3 核心交互逻辑

#### 时间筛选

- 四个 Tab：今日 / 本周 / 本月 / 全部
- 点击切换筛选条件
- 调用 `/order/shop-sale-list` 接口，带 dateRange 参数

#### 订单列表

- 每项显示：单号、会员信息、金额、状态、时间、品项数
- 状态标签：进行中（蓝）/ 已完成（绿）/ 已退款（红）
- 点击列表项 → 跳转订单详情 `/order/:orderNumber`

#### 下拉刷新

- 下拉刷新重新加载列表

### 2.4 字段说明

#### 订单列表项（ShopSaleSummary）

| 字段 | 类型 | 说明 |
|------|------|------|
| orderId | int | 订单 ID |
| orderNumber | string | 订单号 |
| memberId | int | 会员 ID（可为 null）|
| memberName | string | 会员姓名（散客显示"散客"）|
| memberPhone | string | 会员手机号（脱敏）|
| totalAmount | int | 订单总金额（分）|
| payAmount | int | 实付金额（分）|
| status | enum | 状态：`in_progress`/`completed`/`refunded` |
| createdAt | datetime | 创建时间 |
| itemCount | int | 商品品项数 |

### 2.5 异常/边界情况

| 场景 | 处理 |
|------|------|
| 列表为空 | 显示空状态"暂无订单记录" |
| 网络错误 | 显示错误页，点击重试 |
| 状态加载中 | 显示骨架屏 |

### 2.6 跳转关系

| 来源 | 触发 | 目标 |
|------|------|------|
| /home | 点击"订单列表" | /order/list |
| /order/list | 点击列表项 | /order/:orderNumber |
| /order/list | 点击顶部返回 | /home |

---

## 三、页面 2：订单详情

### 3.1 路由

```
路径：/order/:orderNumber
名称：订单详情
参数：orderNumber（订单号）
```

### 3.2 基本布局

```
┌──────────────────────────────────┐
│ ← 订单详情          [⋮ 打印]     │
├──────────────────────────────────┤
│                                  │
│  状态：[已完成]                  │
│                                  │
│  单号：XS202605170001            │
│  时间：2026-05-17 10:30         │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 会员信息                   │  │
│  │ 姓名：李四                 │  │
│  │ 手机：138****8888          │  │
│  │ 等级：黄金会员             │  │
│  └────────────────────────────┘  │
│                                  │
│  商品明细                        │
│  ┌────────────────────────────┐  │
│  │ 📿 足金凤尾纹手镯  30g     │  │
│  │ 单价：¥12,800  数量：1     │  │
│  │ 小计：¥12,800             │  │
│  ├────────────────────────────┤  │
│  │ 💎 50分钻戒 D色 VS1        │  │
│  │ 单价：¥8,600   数量：1     │  │
│  │ 小计：¥8,600              │  │
│  └────────────────────────────┘  │
│                                  │
│  ─────────────────────────────── │
│  合计：¥21,400.00               │
│  优惠：-¥200.00（优惠券）       │
│  实付：¥21,200.00              │
│  ─────────────────────────────── │
│                                  │
│  支付方式：微信支付              │
│  支付时间：2026-05-17 10:32     │
│                                  │
│  [打印小票]                      │
│                                  │
└──────────────────────────────────┘
```

### 3.3 核心交互逻辑

#### 订单状态

- `in_progress`（进行中）：订单创建，待支付/待发货
- `completed`（已完成）：支付完成，订单完成
- `refunded`（已退款）：已退款

#### 商品明细

- 列出订单所有商品
- 每项显示：商品名称、规格、单价、数量、小计

#### 优惠信息

- 显示优惠券抵扣金额
- 显示整单折扣金额

#### 打印小票

- 点击"打印小票"按钮
- 调用蓝牙打印接口，打印小票

### 3.4 字段说明

#### 订单详情（ShopSaleDetail）

| 字段 | 类型 | 说明 |
|------|------|------|
| orderId | int | 订单 ID |
| orderNumber | string | 订单号 |
| memberId | int | 会员 ID（可为 null）|
| memberName | string | 会员姓名 |
| memberPhone | string | 会员手机号（脱敏）|
| memberLevel | string | 会员等级 |
| totalAmount | int | 订单总金额（分）|
| discountAmount | int | 优惠金额（分）|
| payAmount | int | 实付金额（分）|
| couponId | int | 优惠券 ID（可选）|
| couponName | string | 优惠券名称 |
| status | enum | 状态 |
| paymentMethod | string | 支付方式 |
| paidAt | datetime | 支付时间（可选）|
| createdAt | datetime | 创建时间 |
| items | List<OrderItem> | 商品明细 |

#### 订单商品项（OrderItem）

| 字段 | 类型 | 说明 |
|------|------|------|
| productId | int | 商品 ID |
| productName | string | 商品名称 |
| spec | string | 规格（可选）|
| unitPrice | int | 单价（分）|
| quantity | int | 数量 |
| subtotal | int | 小计（分）|

### 3.5 异常/边界情况

| 场景 | 处理 |
|------|------|
| 订单不存在 | 显示错误页"订单不存在" |
| 网络错误 | 显示错误提示，可重试 |
| 打印失败 | Toast "打印失败，请重试" |
| 无会员信息 | 会员信息显示"散客" |

### 3.6 跳转关系

| 来源 | 触发 | 目标 |
|------|------|------|
| /order/list | 点击列表项 | /order/:orderNumber |
| /order/:orderNumber | 点击"打印小票" | 蓝牙打印 |
| /order/:orderNumber | 点击顶部返回 | /order/list |

---

## 四、模块数据流

```
订单列表 → 订单详情

API 调用序列：
1. GET  /order/shop-sale-list      → 获取订单列表（支持日期筛选）
2. GET  /order/shop-sale-info/:orderNumber  → 获取订单详情
```

---

## 五、接口清单

> **注意**：金额字段单位为分（cent），非元。数量字段为整数。

| 页面 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 订单列表 | `/order/shop-sale-list` | GET | 订单列表（支持日期筛选）|
| 订单列表 | `/order/shop-sale-count` | GET | 订单数量统计 |
| 订单详情 | `/order/shop-sale-info/:orderNumber` | GET | 订单详情 |
| 订单详情 | `/order-product/list?orderID=:orderId` | GET | 订单商品列表 |

---

## 六、待确认事项

1. 订单状态的具体枚举值定义
2. 退款订单的展示逻辑
3. 打印小票的格式模板