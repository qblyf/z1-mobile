# 退货退款模块 · PRD

> **模块**：退货退款
> **版本**：v1.2
> **日期**：2026-05-28
> **状态**：待开发
> **依据**：`z1-mid/src/types/return-refund-application-types.ts` + 后端接口分析
> **⚠️ 类型文件**：`lib/types/api/return-refund-application-types.dart` **待生成**（需从后端 TS 翻译）

---

## 一、页面路径总览

```
退货退款
├── /order/return-list           → 退货退款列表
│         ↓
│   /order/return/:id           → 退货退款详情
│         ↓
│   /order/return/:id/audit     → 审核页面
│
└── 关联入口：订单详情页显示"退款"标签
```

**⚠️ 重要说明**：
- 退货退款是独立的申请流程
- 销售订单详情页显示退款标签，可跳转查看退款详情
- 退款状态：`applied`→`audited`→`finished`

---

## 二、退货退款列表

### 2.1 路由

```
路径：/order/return-list
名称：退货退款
父级：订单 Tab 或首页
```

### 2.2 基本布局

```
┌──────────────────────────────────┐
│ ← 退货退款                       │
├──────────────────────────────────┤
│                                  │
│  状态筛选：                      │
│  [全部] [待审核] [已通过] [已拒绝]│
│                                  │
│  ┌────────────────────────────┐  │
│  │ 单号：TH202605170001      │  │
│  │ 原订单：XS202605170001    │  │
│  │ 会员：李四                │  │
│  │ 金额：¥2,000.00          │  │
│  │ 状态：[待审核]            │  │
│  │ 原因：商品有瑕疵          │  │
│  │ 时间：2026-05-17 14:30  │  │
│  └────────────────────────────┘  │
│                                  │
└──────────────────────────────────┘
```

### 2.3 核心交互逻辑

#### 状态筛选

| Tab | 说明 |
|-----|------|
| 全部 | 所有退货退款申请 |
| 待审核 | status = 'applied' |
| 已通过 | status = 'audited' |
| 已拒绝 | status = 'canceled' |
| 已完成 | status = 'finished' |

#### 列表项

- 点击列表项 → 跳转退货退款详情
- 支持下拉刷新、上拉加载更多

---

## 三、退货退款详情

### 3.1 路由

```
路径：/order/return/:id
名称：退货退款详情
参数：id（退货退款申请 ID）
```

### 3.2 基本布局

```
┌──────────────────────────────────┐
│ ← 退货退款详情         [审核]    │
├──────────────────────────────────┤
│                                  │
│  状态：[待审核]                  │
│                                  │
│  单号：TH202605170001            │
│  原订单：XS202605170001          │
│  时间：2026-05-17 14:30          │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 会员信息                   │  │
│  │ 姓名：李四                 │  │
│  │ 手机：138****8888          │  │
│  └────────────────────────────┘  │
│                                  │
│  退货商品                        │
│  ┌────────────────────────────┐  │
│  │ 📿 足金凤尾纹手镯  1件    │  │
│  │ 原价：¥12,800             │  │
│  └────────────────────────────┘  │
│                                  │
│  ─────────────────────────────── │
│  退款金额：¥2,000.00            │
│  ─────────────────────────────── │
│                                  │
│  退货原因：商品有瑕疵            │
│                                  │
│  退货描述：佩戴一周后发现表面     │
│  有明显划痕，影响佩戴。          │
│                                  │
│  [附件图片 x 3]                 │
│                                  │
└──────────────────────────────────┘
```

### 3.3 审核操作

```
┌──────────────────────────────────┐
│ ← 审核退货退款                   │
├──────────────────────────────────┤
│                                  │
│  单号：TH202605170001            │
│  原订单：XS202605170001          │
│  退款金额：¥2,000.00            │
│                                  │
│  ┌────────────────────────────┐  │
│  │ 审核结果                   │  │
│  │ ○ 同意退货退款            │  │
│  │ ○ 拒绝退货退款            │  │
│  └────────────────────────────┘  │
│                                  │
│  审核意见：                      │
│  ┌────────────────────────────┐  │
│  │                            │  │
│  └────────────────────────────┘  │
│                                  │
│        [提交审核]                │
│                                  │
└──────────────────────────────────┘
```

---

## 四、类型定义

### 4.1 退货退款申请状态

```dart
enum ReturnRefundApplicationStatus {
  applied('applied', '已申请'),    // 待审核
  audited('audited', '已审核'),    // 已通过
  canceled('canceled', '已取消'),  // 已拒绝
  consulting('consulting', '协商中'),
  finished('finished', '已完成');
}
```

### 4.2 退货退款申请

> **⚠️ 重要**：以下类型定义参考 `lib/types/api/return-refund-application-types.ts`（唯一真实源）

```dart
// 类型文件：z1-mid/src/types/return-refund-application-types.ts

class ReturnRefundApplication {
  final int id;                    // 退货退款申请 ID
  final String number;             // 退货退款申请单号
  final String orderNumber;         // 销售订单号
  final List<MallOrderInfo> info;   // 退货商品/服务详情（MallOrderProductInfo/MallOrderServiceInfo/MallOrderNonStandardInfo）
  final String reason;              // 退货原因
  final List<Description> description; // 退货描述
  final List<String> images;        // 退货图片
  final String? remarks;           // 备注
  final String createdBy;           // 创建人（UserIdent）
  final int createdAt;             // 创建时间
  final String? updatedBy;         // 更新人（UserIdent）
  final int? updatedAt;            // 更新时间
  final ReturnRefundApplicationStatus status; // 状态
  final int expectedCent;          // 预计退款金额（分）
  final bool isRefundOnly;         // 是否仅退款
}

class Description {
  final String createdBy;         // 发起人（UserIdent）
  final int createdAt;            // 创建时间
  final String content;           // 描述内容
  final bool isCustomer;           // 是否是顾客
}
```

---

## 五、接口清单

| 页面 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 退货退款列表 | `/return-refund-application/list` | GET | 退货退款申请列表 |
| 退货退款数量 | `/return-refund-application/count` | GET | 退货退款申请数量统计 |
| 退货退款详情 | `/return-refund-application/list` | GET | 获取单个详情（带 id 参数）|
| 商城退货退款 | `/return-refund-application/mall` | GET | 商城退货退款列表 |
| 创建退货退款 | `/return-refund-application/add` | POST | 创建退货退款申请 |
| 审核退货退款 | `/return-refund-application/audit` | POST | 审核通过 |
| 驳回退货退款 | `/return-refund-application/reject-audit` | POST | 审核拒绝 |
| 完成退货退款 | `/return-refund-application/complete` | POST | 退款完成 |
| 取消退货退款 | `/return-refund-application/cancel` | POST | 取消申请 |
| 顾客同意退货 | `/return-refund-application/customer/agree` | POST | 顾客同意退货 |
| 顾客添加描述 | `/return-refund-application/customer/add-descript` | POST | 顾客添加描述 |

---

## 六、状态流转

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│  [已申请] ──→ [已审核] ──→ [已完成]                │
│     ↓              ↓                                 │
│  [已取消]    [协商中] ──→ [已申请]                  │
│                                                     │
└─────────────────────────────────────────────────────┘

审核通过：
  applied → audited → finished

审核拒绝：
  applied → canceled
```

---

## 七、异常/边界情况

| 场景 | 处理 |
|------|------|
| 列表为空 | 显示空状态"暂无退货退款申请" |
| 订单不存在 | 显示错误页"订单不存在" |
| 网络错误 | 显示错误提示，可重试 |
| 审核失败 | Toast "审核失败，请重试" |
| 已审核状态 | 隐藏审核按钮 |

---

## 八、待确认事项

1. 退款金额计算规则（是否扣除优惠？）
2. 仅退款 vs 退货+退款的区分逻辑
3. 审核通过后是否需要手动完成退款？
4. 退货商品是否需要扫码入库？

---

## 九、模块关联图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            退货退款模块关联                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│    ┌──────────┐     ┌──────────┐     ┌──────────┐                        │
│    │ 订单详情 │────→│ 退款标签 │────→│ 退款详情 │                        │
│    └──────────┘     └──────────┘     └────┬─────┘                        │
│                          ↑               │                              │
│                          │               │ 关联                          │
│    ┌──────────┐          │               ↓                              │
│    │ 商城订单 │──────────┘     ┌──────────┐                              │
│    │ 详情     │──────────────→│ 退货退款 │                              │
│    └──────────┘               │ 列表     │                              │
│                               └────┬─────┘                              │
│                                    │                                    │
│                               ┌──────────┐                              │
│                               │ 审核流程 │                              │
│                               └──────────┘                              │
│                                                                             │
│    ┌──────────┐                                                           │
│    │ 预订单   │─── 预订单退款 ──→ 退货退款                              │
│    └──────────┘                                                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 9.1 模块跳转关系

| 来源模块 | 触发条件 | 目标模块 | 说明 |
|---------|---------|---------|------|
| 订单详情 | 订单已退款 | 退款标签 | 显示退款信息 |
| 退款标签 | 点击查看 | 退款详情 | orderNumber 关联 |
| 订单列表 | 点击退货退款 Tab | 退货退款列表 | — |
| 商城订单详情 | 申请退款 | 退货退款申请 | — |
| 预订单详情 | 申请退款 | 退货退款申请 | — |

### 9.2 数据共享

| 数据 | 来源 | 消费者 |
|------|------|--------|
| `orderNumber` | 销售订单 | 退货退款申请 |
| `mallOrderNumber` | 商城订单 | 退货退款申请 |
| `expectedCent` | 退货退款 | 订单详情退款标签 |
